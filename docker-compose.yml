services:
  cloudmediastorage:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "443:443"
    volumes:
      - ./data:/app/Data
      - ./storage:/app/storage
      - ./certs:/https
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
